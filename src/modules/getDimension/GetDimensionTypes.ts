export type CoordinatesOfMouse = {
  x: number;
  y: number;
};

export type Bg = {
  img: HTMLImageElement;
  x: number;
  y: number;
  w: number;
  h: number;
};

export type CanvasOffset = {
  left: number;
  top: number;
};

export type CoordinatesOfRectangle = {
  A: { x: number; y: number };
  B: { x: number; y: number };
  C: { x: number; y: number };
  D: { x: number; y: number };
};
