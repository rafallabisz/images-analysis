export type Coordinates = {
  x: number;
  y: number;
};

export type Bg = {
  img: HTMLImageElement;
  x: number;
  y: number;
  w: number;
  h: number;
};

export type CanvasOffset = {
  left: number;
  top: number;
};
